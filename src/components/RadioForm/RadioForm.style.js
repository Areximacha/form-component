import styled from '@emotion/styled'

const QuestionText = styled.span`
  font-size: 20px;
`

const RadioWrapper = styled.div`
  padding: 40px 0;
  display: flex;
`

export { QuestionText, RadioWrapper }
